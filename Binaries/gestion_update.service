[Unit]
Description=Updating gestion
Requires=network.target
After=network.target

[Service]
ExecStart=/usr/bin/sudo -i -- /usr/bin/bash -c "cd /opt/gestion; /opt/gestion/Binaries/gestion_update.rb"

[Install]
WantedBy=multi-user.target
