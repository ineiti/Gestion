This package depends on QooxView. There should be a directory holding both directories:

# ls
QooxView  Gestion
# Gestion/Gestion

This will try to download all missing packets (down to ruby), which seems to be:

# sudo apt-get install ruby rubygems libreoffice pdftk psutils sqlite libsqlite3-dev \
  default-jdk gettext